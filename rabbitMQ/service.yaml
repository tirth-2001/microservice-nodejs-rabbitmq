apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
spec:
  selector:
    app: rabbitmq
  ports:
    - protocol: TCP
      port: 5672 # RabbitMQ default port
      targetPort: 5672
  type: LoadBalancer
